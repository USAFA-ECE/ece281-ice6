# Tell git what files to ignore
# start by ignore everything, un-ignore specifics
*

# never ignore itself
!.gitignore

# don't ignore anything with prelab
!*prelab*


# don't ignore the project files
!*.xpr

# don't ignore waveform files
!*.wcfg

# don't ignore hardware description files
!src/
!src/**

# or configuration files
!cfg/
!cfg/**/*.wcfg

# don't ignore bitstream
!*.runs/
!*.runs/**

# don't ignore markdown
!*.md
!*.png
!*.jpg

# don't ignore .github
!.github/
!.github/**

